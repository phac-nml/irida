/**
 * Component that wrap all components that handle metadata templates.  Required
 * for Reach Router nesting.
 *
 * @param children
 * @returns {*}
 * @constructor
 */
export function MetadataTemplates({ children }) {
  return children;
}
