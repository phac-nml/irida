/**
 * Component that wrap all components that handle metadata fields.  Required
 * for Reach Router nesting.
 *
 * @param children
 * @returns {*}
 * @constructor
 */
export function MetadataFields({ children }) {
  return children;
}
