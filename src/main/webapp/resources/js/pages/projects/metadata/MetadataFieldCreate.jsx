import React from "react";
import { PageHeader, Space } from "antd";

/**
 * Component to add a new metadata field to a project
 *
 * @returns {JSX.Element}
 * @constructor
 */
export function MetadataFieldCreate({}) {
  return (
    <PageHeader title={"Create Metadata Field"}>
      <Space>
        <p>This still needs to be implemented</p>
      </Space>
    </PageHeader>
  );
}
