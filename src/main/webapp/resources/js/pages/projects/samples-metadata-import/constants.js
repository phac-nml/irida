/**
 * @file Handles any constants that are used in more than one file.
 */

/**
 * URL fragments for ui.router
 */
export const STATE_URLS = {
  upload: "/upload",
  sampleId: "/sampleId",
  resultTable: "/result"
};
