import React from "react";

/**
 * React component to display the sequencing run list page.
 * @returns {*}
 * @constructor
 */
export default function SequencingRunListPage() {
  return (<div>Sequencing Run List</div>);
}