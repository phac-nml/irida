export const TYPES = {};

const initialState = {
  initialized: false
};

export const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export const actions = {};
