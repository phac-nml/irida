(function (define) {
    'use strict';
    define(['app'], function (app) {
        app.controller('PipelinesCtrl', [function () {
            this.message = 'Message from PipelinesCtrl';
        }]);
    });
})(define);