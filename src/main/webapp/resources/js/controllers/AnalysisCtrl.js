(function (define) {
    'use strict';
    define(['app'], function (app) {
        app.controller('AnalysisCtrl', [function () {
            this.message = 'Message from AnalysisCtrl';
        }]);
    });
})(define);