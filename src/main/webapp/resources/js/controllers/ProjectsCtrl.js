(function (define) {
    'use strict';
    define(['app'], function (app) {
        app.controller('ProjectsCtrl', [function () {
            this.message = 'Message from ProjectsCtrl';
        }]);
    });
})(define);