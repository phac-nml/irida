(function (define) {
    'use strict';
    define(['app'], function (app) {
        app.controller('DashboardCtrl', [function () {
            this.message = 'Message from DashboardCtrl';
        }]);
    });
})(define);