(function (define) {
    'use strict';
    define(['app'], function (app) {
        app.controller('FilesCtrl', [function () {
            this.message = 'Message from FilesCtrl';
        }]);
    });
})(define);